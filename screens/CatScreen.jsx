import { View, Text } from "react-native";

export default function CatScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>I am the cat screen</Text>
      </View>
    );
  }